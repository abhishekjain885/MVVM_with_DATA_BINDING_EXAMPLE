<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
                name="articleViewModel"
                type="com.example.apple.minidemo.viewModel.ArticleListViewModel"/>
    </data>

    <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            tools:context=".activity.NewsSourcesActivity">

        <include layout="@layout/toolbar"/>


        <include layout="@layout/search_bar"/>


        <android.support.v7.widget.RecyclerView
                android:id="@+id/listOfSources"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:setAdapter="@{articleViewModel.getAdapter()}"/>

        <ProgressBar
                style="?android:attr/progressBarStyleLarge"
                android:id="@+id/spinnerLoading"
                android:indeterminate="true"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:visibility="@{articleViewModel.loading}"
                android:layout_gravity="center"/>

        <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:visibility="@{articleViewModel.showEmpty}"
                android:text="No Source were returned."/>

    </LinearLayout>


</layout>